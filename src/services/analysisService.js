// Food image and nutrition label analysis service
import apiClient from './api';

export const analysisService = {
  analyzeImage: async (formData) => {
    console.log('Analyze image stub');
    return { success: true, analysis: null };
  },
  analyzeLabel: async (formData) => {
    console.log('Analyze label stub');
    return { success: true, analysis: null };
  },
};
